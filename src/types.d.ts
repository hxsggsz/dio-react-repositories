export interface userProps {
  bio: string
  name: string
  login: string
  html_url: string
  avatar_url: string
}

export interface repoProps {
  name: string
  language: string
  html_url: string
}